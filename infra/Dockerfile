# This Dockerfile extends the base nominatim image if you need customizations
FROM mediagis/nominatim:5.1

# Add any custom configurations or scripts here if needed
# For example, you could copy custom configuration files:
# COPY nominatim.conf /etc/nominatim/

# Or install additional packages:
# RUN apt-get update && apt-get install -y \
#     your-additional-package \
#     && rm -rf /var/lib/apt/lists/*

# The base image already handles the setup, so no additional commands needed for basic usage
